# 335FinalProject
--Render URL-- https://three35finalproject-ef3t.onrender.com  
**NOTICE**  
Original Render URL no longer functions. Please download the complete repository  
and run the Node.js server locally using npm. Open command prompt, ensure npm is installed,
and use the command 'npm start'. The site can be accessed through:  
localhost:3000
